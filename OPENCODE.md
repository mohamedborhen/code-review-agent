# OpenCode Status Tracker

This file is maintained by OpenCode agents. Agents MUST update this file after completing significant milestones or when encountering blocked tasks. **If you hit an ambiguity not resolved in `AGENTS.md` or the current phase file, log it below and stop — do not guess.**

## 🚀 Current Active Task
- [x] Phase 2 build + self-audit complete (see DoD below for the one blocked item). Next: real credentials + live E2E once granted.
- [x] Multi-provider model selection implemented: `review_model` (env `REVIEW_MODEL`) now accepts any `provider:model` spec via langchain's native `init_chat_model` dispatch — no custom factory.
- [x] Real credentials granted and `POST /review` happy path exercised end to end (Stages 0–6 of `PHASE_2_TEST_PLAN.md` all PASS; Stage 4 partially — see the compliance-retrieval gate under Blockers).
- [x] Stage 7 fixes applied + re-verified: (A) Compliance `context7` → `set()` (12 tools, matches PHASE_2.md); (B) lenient per-agent report coercion + real per-subagent `duration_ms` via `CaptureStore` (unit-tested; sessions 13/14 show real durations, aggregator 40578/50812 ms, compliance 2796/3093 ms); (C) subagent-internal MCP `tool_call`/`tool_result` events now emitted to `review_events.log` per owning agent (session 12 proved compliance's `confluence_search`/`confluence_get_page`/`get_review_context_tool`/`pull_request_read`); plus a bounded retry in `run_review` for transient deepagents structured-output parse failures (weak-model empty-output 500 observed once, gone after fix).
- [ ] Phase 3 sign-off (gate the remaining blockers below; reviewer final pass = 29/30, one minor FAIL now FIXED). **Reviewer DoD audit (20-item) complete: 19 PASS / 1 PARTIAL (cosmetic) — see Changelog 2026-08-11; no hard blockers, deviations D-1..D-7 logged above.**
- [x] **Supervisor-demo warm-up green on NVIDIA Nemotron 49B** (sessions 34–40): `security_question` E2E produces real findings + per-agent evidence (CRG + GitHub tool calls, populated audit rows). Fix chain logged in Changelog. **Next:** full `request_type=review` (all 4 specialists + aggregator), then `any_question` variants.
- [x] **Phase 4 Layer 5/2 (infra_engineer pass) complete: memory store + LangMem wiring + truncation fix + durable summaries** — see Changelog 2026-08-18. Full suite `72 passed` (62 baseline + 10 new). Reviewer audit pending.
- [x] **Phase 4 reviewer audit COMPLETE: 11/11 §8 DoD items PASS + all §2 compliance constraints met (3 log-only deviations, no blockers)** — see Changelog 2026-08-19. DoD COMPLETE.
- [x] **Post-audit review-finding fixes applied (2026-08-19): F1 (ProviderProfile max_tokens/timeout on the summary LLM), F2 (durable summary → FastAPI BackgroundTask, off the request path), F5 (to_thread on the use-case's sync store calls); F3/F4 accepted as documented tradeoffs** — see Changelog 2026-08-19. Suite now **`74 passed`** (was 72). Remaining for sign-off: commit the working tree.

## 🚧 Blockers / Pending Questions
- ~~**Orchestrator-path context audit misreports large recalls (Phase 3 observability — DEFERRED by user decision 2026-08-18, logged not fixed):** `scope_agent_tools`'s event wrapper truncates tool results to 4000 chars (`tool_scoping.py` `_truncate_result`/`_wrap_with_events`, lines 37-43); `get_audited_context_tool`'s `_audited` then `json.loads` the possibly-truncated payload, so a SUCCESSFUL recall whose JSON exceeds 4000 chars (realistic at `limit`≈25 with long snippets) is recorded as `status="invalid_response"` / `results_count=0`. Turn-flow path is unaffected (raw scoped tool, no truncation — parses correctly). Options when revisited: parse before truncation, or regex-count `message_id` when the truncation marker is present.~~ **RESOLVED (Phase 4 §0, 2026-08-18):** `tool_scoping.py` now returns tool results to the caller FULLY untruncated (event-log copy stays capped at 2000 chars, observational only). Large recalls parse correctly — unit-tested with a 5000-char payload (`status="ok"`, count=25).
- **Auth + User table — PENDING QUESTION for the Phase 4+ spec (not a code defect):** the codebase has **no auth middleware and no `User` table**. `user_id` is a free-form, caller-supplied string in the request body (`POST /conversations`, `POST /conversations/{id}/message`, `POST /review`); the only enforcement is `search_messages`' server-side `conversation_id+user_id+repo_id` match (`conversation_server.py` `_authorized`), i.e. `user_id` is a **tenant-scope key, not an authenticated principal** (PHASE_3.md §1/§9.5). A productized system needs sign-in/login (authenticated identity, e.g. JWT claims) and a `User` table (accounts, conversation ownership, Phase-4 Private/Shared-memory owner scoping). This was flagged early in `PHASE_1_SUMMARY.md:647` ("auth middleware" row). It is **intentionally out of PHASE_1–3 scope** (Phase 4's contract `PHASE_3.md` §20 covers only LangMem / private memory / summarization) — the future phase spec must decide the identity source; do not implement speculatively.
- **mcp-atlassian launch env is load-bearing (OPS NOTE, incident 2026-08-18 / session 115):** a bare `uvx mcp-atlassian --transport streamable-http --port 9000` — without the `run_atlassian_server.sh` exports (`ALLOW_GLOBAL_CRED_FALLBACK=true`, `READ_ONLY_MODE=true`, `TOOLSETS=all`, `ENABLED_TOOLS=...`) and `--env-file .env` — makes `UserTokenMiddleware` 401 every MCP request, so `get_tools(server_name="atlassian")` raises an ExceptionGroup and `POST /review` 500s ("unhandled errors in a TaskGroup"). The server banner "FastMCP 3.4.7" is the framework version, NOT mcp-atlassian's — the package is `0.23.0`, matching Phase 2 (verified `uvx mcp-atlassian --version`). Script now pins `mcp-atlassian@0.23.0`; always launch via the script or the compose service, never a bare uvx call.
- **Per-agent `*_MODEL` env vars / multi-provider model factory:** still NOT built — confirmed deferred. Switching the *whole system's* single model between providers (groq/openrouter/google_genai/openai/anthropic/...) is now a supported, implemented feature via `REVIEW_MODEL` + `init_chat_model` native dispatch (provider integration package + API key per provider, see `requirements.txt` and `.env.example`). A custom `model_factory.py` abstraction and per-agent `*_MODEL` env vars remain unwanted unless explicitly requested; if that day comes, deepagents' native per-subagent `"model"` field covers it without custom code.
- **`datetime.utcnow()` usage — PARTIALLY STALE (updated by reviewer DoD audit):** The DB tables were already fixed: committed `db/models.py` uses `datetime.now(timezone.utc)` for `RepoWorkspace`/`GraphSnapshot`/`ReviewSession`/`AgentExecution` (verified via `git show HEAD:.../db/models.py`; `db/models.py` is on Phase 2's allowed-modification list, so this was in scope). The ONLY remaining `utcnow` site is the Phase 1 **domain entity** `domain/entities/graph_build_status.py:10` (`field(default_factory=datetime.utcnow)`) — a Phase 1 file that Phase 2 may not touch. Also stale: this entry's "Not yet fixed in Phase 1 files" claim and `PHASE_2.md:540`'s "Not fixed here since it's outside this phase's scope" note both describe the DB tables as unfixed when they are fixed. Recommend: (a) reword `PHASE_2.md:540` to point only at `GraphBuildStatus`; (b) schedule the domain-entity fix as a Phase 3 cross-phase cleanup (single-line: swap to `datetime.now(timezone.utc)`).
- **Compliance-retrieval gate — PARTIALLY MET (updated after Fix C):** The observability half is now closed: subagent-internal MCP calls are captured per owning agent via the tool wrapper in `tool_scoping.py` (session 12 event log proves compliance called `confluence_search`("Team Coding Standards") + `confluence_get_page`(360710) + `get_review_context_tool` + `pull_request_read` for real — retrieval happens, it is not silently failing). What REMAINS open is citation fidelity: compliance's one E2E citation — "Confluence 'Code Quality Guidelines' (Section 3.1: No unused code or placeholders...)" — does NOT match real page 360710 content (no such heading/section/text), i.e. the model grounds on real pages but fabricates the specific section reference. That is an LLM-fidelity limitation of the weak `gpt-oss-20b` review model, out of Phase 2's scope to fix. CARRY to Phase 3 (stronger model or a citation-verification/grounding step).
- **Per-agent `AgentExecution` rows lose real findings (data-fidelity):** ~~`orchestrator_runtime._parse_tool_message` (`:147–156`) strict-parses each `task` ToolMessage as `SubagentReport` and falls back to empty `findings` on any deviation. E2E session 11: performance/regression sent string `confidence:"high"` (schema needs `float`) and evidence-as-list-of-dicts (schema needs `list[str]`); compliance emitted a `violations` key (schema needs `findings`). Result: rows recorded with `{"agent_name":..., "findings":[]}` while the event log + aggregator retain the real data. `duration_ms=0` is hardcoded for subagent rows (`review.py:103`). DoD letter is met (rows written; `asdict` mechanism correct) but the audit-trail purpose is degraded. CARRY.~~ **FIXED (Stage 7).** `_coerce_report`/`_coerce_finding` in `orchestrator_runtime.py` recover string confidences (low/medium/high/critical → 0.3/0.6/0.9/1.0), dict evidence → JSON string, and a `violations` top-level key → `findings`; `_parse_tool_message` routes straight to coercion when `violations` is present (pydantic's default extra-ignore otherwise makes strict validation "succeed" with empty findings); `CaptureStore` + `SubagentCaptureMiddleware` record real per-subagent `duration_ms` (was hardcoded 0). Unit-verified against the exact session-11 failure payloads; live sessions 13/14 show real durations.
- **Compliance granted ALL Context7 tools vs PHASE_2.md (reviewer item 8, minor FAIL):** ~~`tool_lists.py:93` sets `"context7": None` for Compliance (unfiltered), but PHASE_2.md's Compliance table has no Context7 row. This also corrects this file's §28 claim that "all 5 match exactly." One-line fix: Compliance's `context7` → `set()`. Low risk (Context7 is read-only doc lookup).~~ **FIXED (Stage 7):** `tool_lists.py` Compliance `"context7"` → `set()`; live tool dump confirms 12 tools, matches PHASE_2.md.
- **Review-model nondeterminism (Phase 3 concern, not a code defect):** the configured `openrouter:openai/gpt-oss-20b` is a weak model and varies run to run: it sometimes delegates to all four required subagents (session 11), sometimes only one (sessions 13/14), occasionally omits the diff from a `task` call (session 14 — compliance then asked for the PR), and once returned an empty native structured output (session 12 500 — now absorbed by the bounded retry). Pipeline code handles all of these correctly (200, real durations, tool events, coercion); the variance is output quality. If Phase 3 needs deterministic multi-agent coverage, switch `REVIEW_MODEL` to a stronger model and/or make the orchestrator prompt enumerate exact required task calls.
- **deepagents harness-profile skip for multi-colon model specs (constraint, resolved-by-avoidance):** `deepagents` `_get_harness_profile` returns `None` when `spec.count(":") > 1` (`harness_profiles.py:1078–1084`), silently skipping the safety profile (built-in write/execute tool stripping + disabled general-purpose subagent). `openrouter:openai/gpt-oss-20b:free` hit this (crash: "Profile key ... has more than one ':'"). Fixed by using the single-colon spec `openrouter:openai/gpt-oss-20b` (verified: profile resolves, all 8 built-ins excluded, general-purpose subagent disabled). Any future model id containing a second `:` (e.g. `:free`, `:nitro`) will silently lose the harness — document/validate at config time if such models are ever used.
- **Cross-phase (Phase 1): `psf/requests` workspace errored with `WinError 5`** on a git pack during clone — graph for that repo stays `error`; used as the 425 test case. Not Phase 2 scope.
- **RESOLVED — `psf/requests` base clone's git remote was `mohamedborhen/code-review-agent.git`, not `psf/requests`** (previously blocked the Branch-Aware addendum's live worktree happy-path E2E). The remote URL was corrected to `https://github.com/psf/requests.git` (verified). The worktree happy path is now exercised live end to end — see the Branch-Aware section.
- **Phase 2 live E2E blocked on real credentials:** ~~`REVIEW_MODEL`/provider key and `GITHUB_PAT` are placeholders by design, and mcp-atlassian cannot be launched without a Confluence instance.~~ **RESOLVED** — real credentials granted, all services up, E2E ran (see Current Active Task). Kept for history; the remaining open items are the four blockers above.
- **GitHub MCP tool names unverified against the live registry:** ~~`infrastructure/agents_runtime/tool_lists.py` transcribes PHASE_2.md's names but carries an explicit DO-NOT-TRUST warning.~~ **RESOLVED** — Stage 2 dumped the live GitHub server tool list (30 tools, zero write-capable names) and each agent's runtime-scoped tools matched PHASE_2.md; DoD item checked with evidence.
- **Reviewer DoD audit — deviations (no hard blockers; fix before Phase 3 sign-off):**
  - **D-2: `config.py:36-40` comment is stale/contradicts the code.** The comment block above `review_max_tokens` says "No default cap exists in code; deepagents/openrouter resolve 'unset' to the model's full output window", but the field has `int = 8192` in the working tree (and `int = 2000` in committed HEAD `8c1dcbe`). The working-tree change `2000 → 8192` is UNCOMMITTED (see D-6). Fix: either commit the 8192 default, or if the intent is "no cap", drop the default entirely (`int | None = None`) and reconcile the comment. `PHASE_2.md:592` and `.env.example:17` (`REVIEW_MAX_TOKENS=` blank) should match whichever is chosen.
  - **D-3: Mojibake in `subagents/fix_suggestion_runtime.py:3`.** Line 3 contains a corrupted em-dash stored as bytes `EF BF BD 3F 22` rendering as `�?"` in "apply_refactor_tool �?" — see AGENTS.md". Note: the file lives at `infrastructure/agents_runtime/subagents/fix_suggestion_runtime.py` (the `subagents/` subdir), not directly under `agents_runtime/`. Cosmetic but committed; fix to `—`.
  - **D-4: Stray scratch script `create_issue.py` at repo root is committed and now modified.** It's a Jira-issue-creation demo/diagnostic script (reads real credentials from `.env`, which is gitignored — no leak, but the script is demo cruft). Working tree has a new modified version (CLIP issue "Extract duplicated traffic-light grade computation"). Recommend: delete it or move it out of the repo root into a scratch/ dir, and confirm no `.env` path it reads is tracked.
  - **D-5: Working-tree `.gitignore` adds `.opencode/`, but `.opencode/agents/*.md` ARE tracked.** `git ls-files` shows `.opencode/agents/domain_architect.md`, `infra_engineer.md`, `reviewer.md` are committed. Adding `.opencode/` to `.gitignore` (uncommitted) is misleading — it only affects *new* files (the tracked ones remain tracked), silently dropping future agent `.md` files from version control. If the intent is to ignore `.opencode/node_modules`/package artifacts, pattern them narrowly instead.
  - **D-6: Uncommitted working tree — the audit reviewed working tree, not HEAD.** `git status` shows modified: `.gitignore`, `capture.py`, `middleware.py`, `orchestrator_runtime.py`, `config.py` (8192), `create_issue.py`; deleted: `test_aggregator_parse.py`, `test_nvidia.py`. These are the Stage-7+ demo fixes (capture/middleware/orchestrator) plus the D-2/D-4/D-5 changes. HEAD `8c1dcbe` does NOT contain them. The DoD audit passed against the working tree; before Phase 3 sign-off the working tree must be committed (or intentionally reverted) so HEAD reflects what was verified.
  - **D-7 (informational, not a defect): `config.py:78-79` `load_dotenv` runs AFTER `settings = Settings()` (line 76).** Not a pure no-op: it populates `os.environ` for non-Settings consumers (`init_chat_model` reads provider keys from env; mcp-atlassian server env), so it's intentional for that purpose — but Settings itself never benefits (it's already constructed from the process env / `env_file=".env"` relative to CWD). Note the path asymmetry: pydantic-settings uses CWD-relative `.env` while `load_dotenv` uses `_APP_ROOT / ".env"` — if uvicorn starts from a different CWD than `_APP_ROOT`, the two can read different files. Consider loading dotenv before constructing Settings and using one canonical path.

## ✅ Phase 1 — COMPLETE (archived, do not re-open)
- [x] `requirements.txt`, domain layer, SQLModel tables, workspace cloning, webhook handling, signature validation, branch-deletion skip, `filelock` concurrency, `build_or_update_graph_tool` integration, retry policy, eviction policy, CRG-redundancy fix (in-app subprocess launch removed, Docker networking corrected, healthcheck + `service_healthy` added) — full detail in `PHASE_1.md`.

## ✅ Definition of Done — Phase 2 (current)

### Incoming Request & Pre-Flight
- [x] `POST /review` validates its body against `ReviewRequest` (Pydantic, `infrastructure/api/models.py` — not `domain/`).
- [x] `PrepareReviewContextService.execute` runs before any agent dispatch: 404 for unknown `repo_id`, 425 if the graph isn't ready for `graph_commit_hash` — both failure cases actually tested, not just the happy path. (Exercised live via TestClient: 400 unknown request_type, 404 unknown repo_id, 425 unready commit — all returned the documented status/detail.)
- [x] Every subagent's `repo_root` comes from `RepoWorkspace.local_path` via `PrepareReviewContextService` — never guessed or constructed. (Grep confirmed Phase 2 code never builds a path from `repo_id`; the only path sources are Phase 1's `clone_repository.py`/`sync_on_webhook.py`, out of scope.)

### Orchestration
- [x] All 7 agents (Orchestrator, Compliance, Security, Performance, Regression, Fix Suggestion, Aggregator) constructed via `create_deep_agent`, using plain dict literals for subagents (not a typed class). Note: Orchestrator + Aggregator are the root agent's classify/synthesize phases (no MCP tools); the 5 subagents are registered as plain dicts on `create_deep_agent(subagents=...)`. For `explain_question` (empty routed set) `subagents=None` — no subagents constructed for a request that can't use them.
- [x] Each subagent's `tools` list built by filtering `MultiServerMCPClient.get_tools()` output to exactly the tool names specified per agent in `PHASE_2.md` — no agent has a tool outside its assigned set. (Cross-checked each plan against PHASE_2.md's Per-Agent tables: all 5 match exactly.) **CORRECTION (Stage 7): Compliance additionally gets ALL Context7 tools (`tool_lists.py:93`, `"context7": None`) though PHASE_2.md's Compliance table has no Context7 row — a minor deviation, logged under Blockers with a one-line fix.**
- [x] Fix Suggestion has `refactor_tool` but explicitly does NOT have `apply_refactor_tool`.

### Routing & Contracts
- [x] Routing Policy implemented, and includes `performance` in the `review` entry.
- [x] `AgentFinding`/`AgentOutput` contracts implemented exactly as specified.
- [x] `AgentExecution.result` populated via `json.dumps(dataclasses.asdict(agent_output))`, not raw `json.dumps(agent_output)`.

### MCP Connections
- [x] `MultiServerMCPClient` configured for all four servers: `crg`, `github`, `atlassian`, `context7`.
- [x] `MultiServerMCPClient` constructed once at FastAPI startup (lifespan), not per-request.
- [x] The `crg` entry of `MultiServerMCPClient` uses `settings.crg_server_url` (Phase 1's env-configurable setting), not a hardcoded `127.0.0.1` URL — required so the Docker deployment's `http://crg-server:5555/mcp` override keeps working.
- [x] `mcp-atlassian` configured via `uvx mcp-atlassian --transport streamable-http --port 9000` (script) / `TRANSPORT=streamable-http, PORT=9000` (compose service) — not the official Rovo server. Live launch still blocked on a Confluence instance (see Blockers).
- [x] GitHub auth via PAT (`Authorization: Bearer`), not OAuth.
- [x] GitHub MCP client headers include `"X-MCP-Readonly": "true"` and a scoped `"X-MCP-Toolsets"` value — read-only enforced server-side, not just via client-side filtering.
- [x] No subagent's GitHub tool list is (or reduces to) "all GitHub tools" — Compliance, Security, Performance, and Regression each have an explicit named tool list matching `PHASE_2.md`'s Per-Agent Breakdown tables.
- [x] Those GitHub tool names were checked against the live GitHub MCP tool registry (e.g. `mcpcurl tools --help`) before implementation, not copied from `PHASE_2.md` unverified. **DONE (Stage 2):** live GitHub server tool list dumped (30 tools, zero write-capable names) and each agent's runtime-scoped tools matched PHASE_2.md exactly — resolving the prior BLOCKED item.
- [x] `handle_tool_errors` left at its default (`True`) everywhere — not overridden to `False`.
- [x] `tool_name_prefix` left at its default (`False`) — no prefix-stripping logic anywhere in `scoped()`.

### API & Data
- [x] `POST /review` runs pre-flight → orchestrator → subagents (per routing policy) → aggregator, returns a structured result. (Implementation complete; happy path E2E ran live: session 11 → 200, aggregator 9 findings, all four `review` agents delegated via the root `task` tool — see Blockers for the compliance-retrieval caveat.)
- [x] A failed review still writes an `AgentExecution` error row before returning a 500 — verified by forcing a real failure, not just reading the code. (Exercised live: ready commit + placeholder model → GitHub tool-list 400 → 500 + `AgentExecution(agent_name='orchestrator', result={'status':'error',...})` row committed.)
- [x] `ReviewSession` and `AgentExecution` tables added to the existing Phase 1 SQLite DB (same DB, not a new one), using timezone-aware timestamps (`datetime.now(timezone.utc)`), not naive ones. Caveat recorded in PHASE_2.md: the SQLite round-trip stores the wall time without the offset (SQLAlchemy sqlite dialect), so cross-table naive/aware comparisons remain a scheduled cross-phase cleanup item.
- [x] Phase 2's new tables (`ReviewSession`, `AgentExecution`) actually exist at runtime — already satisfied by Phase 1's `init_db()`/`create_all` (called in `main.py` lifespan); Phase 2's action is adding the tables to `db/models.py`, which `init_db()` imports wholesale. Verified live: `journal_mode=wal`, tables `['agentexecution','graphsnapshot','repoworkspace','reviewsession']`.
- [x] `db/engine.py` sets `PRAGMA journal_mode=WAL` and `PRAGMA busy_timeout=5000` on connection.
- [x] Event schema (thinking/tool_call/tool_result/final) logged to stdout/file — no UI required yet. (Verified entries written to `logs/review_events.log`.)

### Config
- [x] `review_model` read from `settings`, no hardcoded default anywhere in code, set via the `REVIEW_MODEL` env var. Provider-agnostic: any `provider:model` spec resolvable by langchain's `init_chat_model` (verified native support for groq/openrouter/google_genai/openai/anthropic/deepseek/ollama/etc.); the provider's package + API key env var are the only per-provider requirements. The safety harness profile is registered under the exact `settings.review_model` key, and deepagents resolves it exact-spec-first, so excluded-tools + disabled general-purpose subagent hold for any provider.

### Layer Discipline
- [x] Domain layer has zero imports of `deepagents`, `langchain_mcp_adapters`, `pydantic` (Pydantic belongs to `infrastructure/api/models.py`, not `domain/`), or `fastapi`.
- [x] Phase 2 ports are `async def`; the one sync exception (the `PrepareReviewContextService` pre-flight composing Phase 1's sync `GraphReadinessService` + two Layer 5 SQLModel adapters, called directly from the async route) is documented, not silently spread elsewhere.
- [x] Nothing from Phase 1's folder tree was deleted or modified except `config.py`, `db/models.py`, `db/engine.py` (all extended, not replaced). Additive note: `main.py` gained a review-router include + a single `app.state.mcp_client = build_mcp_client()` line in the existing lifespan (required wiring for this phase), and `requirements.txt`/`.env(.example)` gained Phase 2 entries — all strictly additive, no Phase 1 logic touched.

### Scope
- [x] No LangMem, no Context Agent, no conversation persistence tables, no frontend, no streaming, no multi-provider model factory (see Blockers).

## 📝 Changelog / Progress Notes
- *[System]* — `domain_architect.md`, `infra_engineer.md`, `reviewer.md` model frontmatter corrected from `deepseek-v4-flash` to `opencode/deepseek-v4-flash-free` (the original id did not resolve in this environment; "Model not found" on task launch). NOTE: the running session's agent config is cached from session start, so the named subagents could not be launched mid-session despite the file fix — Phase 2 was executed inline by the orchestrator in the mandated order (domain → infra → review) with the same rules. Future sessions will launch the subagents normally.
- *[System]* — Phase 1 archived complete, including the CRG-redundancy fix. Phase 2 tracker synced against the current `PHASE_2.md` (post `ReviewRequest`/readiness/path-resolution additions). Two items logged as open blockers above.
- *[System]* — `PHASE_2.md` corrected for three contradictions with the actual Phase 1 code: (1) table creation is already wired via Phase 1's `init_db()`/`create_all` in the lifespan, so Phase 2 needs no new startup wiring — just add the new tables to `db/models.py`; (2) the CRG entry of `MultiServerMCPClient` must use `settings.crg_server_url`, not a hardcoded `127.0.0.1` URL, or the Docker deployment breaks; (3) the Phase 2 DoD's allowed-modifications list now includes `db/engine.py`, matching `AGENTS.md` and the folder tree. `infra_engineer.md`, `reviewer.md`, `domain_architect.md`, and `AGENTS.md` synced to the same corrected reality (infra_engineer item 1 now requires `settings.crg_server_url`; its item 6 no longer says `create_all` was never wired; reviewer gained item 22 for the CRG URL and item 25 rewording the create_all check; domain_architect's description of `graph_readiness_service` corrected to the port-injected design; AGENTS.md gained standing rules for both).
- *[System]* — `PHASE_2.md` revised: GitHub MCP config now sets `X-MCP-Readonly`/`X-MCP-Toolsets` headers, and Compliance/Security/Performance/Regression each got explicit GitHub tool lists replacing the earlier "all GitHub tools" grant. `reviewer.md`, `infra_engineer.md`, and this file's Definition of Done were synced accordingly; `AGENTS.md`'s Safety & Correctness Rules gained a standing cross-phase rule for the same change. `domain_architect.md` reviewed — no change needed, since it doesn't touch MCP tool scoping.
- *[System]* — **Phase 2 build complete** (domain → application → infrastructure → API → deploy wiring, run inline in that order). Reviewer's 30-item audit passed except live GitHub tool-name verification (blocked on real PAT). Verifications performed live: import chain; deep-agent construction (graph nodes confirmed); HarnessProfile resolution stripping all 8 built-in tools + disabling the general-purpose subagent; routing edge cases (`[]` vs `None`); WAL + 4 tables; event-log writes; TestClient exercising 400/404/425/500 with a committed error `AgentExecution` row. One code improvement during audit: `scope_agent_tools` now skips `get_tools()` for servers an agent wants zero tools from (`allowed == set()`), so a down mcp-atlassian can't fail a Security/Performance/Regression build that doesn't use it.
- *[System]* — `verify_api.py` left two rows in the local dev DB (`ReviewSession id=1`, `AgentExecution id=1`) as evidence of the forced-failure test; harmless local data, not committed.
- *[System]* — **Multi-provider model selection implemented.** `settings.anthropic_model` renamed to `settings.review_model` (env `REVIEW_MODEL`) in `config.py`, `harness_profile.py`, `orchestrator_runtime.py`; now any `provider:model` spec resolvable by langchain's `init_chat_model` works (verified native provider dispatch for groq/openrouter/google_genai/openai/anthropic + more). `requirements.txt` bundles `langchain-anthropic` (now explicit), `langchain-google-genai>=4.3,<5`, `langchain-openai`, `langchain-groq`, `langchain-openrouter`. `.env`/`.env.example` document `REVIEW_MODEL` + per-provider key vars (only the matching one required). The multi-provider blocker is resolved — via native dispatch, no custom `model_factory.py`, and per-agent `*_MODEL` env vars stay deferred. Docs synced: `AGENTS.md`, `PHASE_2.md` (config note + DoD item), `OPENCODE.md`, `infra_engineer.md`, `reviewer.md`. `domain_architect.md` unchanged (no model-setting references).
- *[System]* — **Phase 2 test-plan execution (Stages 0–6) complete + Stage 7 reconciled.** Results recorded in `PHASE_2_TEST_PLAN.md`. Stage 0: real `REVIEW_MODEL=openrouter:openai/gpt-oss-20b` (single-colon — the `:free` variant crashes deepagents' harness-profile lookup, see Blockers) + services up (CRG 5555, mcp-atlassian 9000 with `READ_ONLY_MODE=true`/`ENABLED_TOOLS` confirmed in process logs, uvicorn 8001, client built once). Stage 1 PASS (single DB, wal, busy_timeout 5000 via engine, tz-aware Python-side). Stage 2 PASS (per-agent tool dumps match PHASE_2.md — except Compliance's extra Context7 grant, see Blockers; `apply_refactor_tool` absent; atlassian server-side rejects write tools; GitHub headers + 30-tool zero-write registry confirmed). Stage 3 PASS (404/425/400/422/200). Stage 4 PARTIAL: full `review` E2E ran (session 11, 200, aggregator 9 findings, all 4 routed agents delegated, event schema correct) BUT the compliance-retrieval gate is unmet (see Blockers) and per-agent `AgentExecution` rows lose findings (see Blockers). Stage 5 PASS (forced-failure error row before 500; 5 concurrent POSTs all 200, no `database is locked`). Stage 6 PASS (domain clean, `asyncio.run` only in Phase 1, git diff baseline `HEAD`=3e1d86e, allowed-modifications respected). Stage 7: reviewer 30-item final pass = **29 PASS / 1 minor FAIL** (item 8, Compliance/Context7). Also: the plan's "keys committed to `.env`" security warning was checked and found FALSE — `.env` is gitignored and untracked (only `.env.example` with placeholders is committed).
- *[User-approved extension]* — **Free-form `any_question` request type added** (supervisor-demo requirement: "ask the multiagent system any question about the repo, it delegates to the relevant specialist(s)"). `routing_policy.py` adds `"any_question": ["compliance","security","performance","regression"]` as an *available pool*; `api/models.py` + `domain/entities/agent_finding.py` gain an optional `question` field (passed through in `review.py`); `orchestrator_runtime._build_user_message` switches to available-pool wording for `any_question` (vs the forced "MUST delegate to each") and embeds the user's question; `prompts/orchestrator.md` gains instruction #4 (delegate to the relevant specialist(s) — one, several, or none — or answer directly). No new tools, no new libraries, no safety-rule changes; capture/audit/event-log already handle whichever subset actually runs.
- *[System]* — **Stage 7 blocker fixes applied + re-verified live.** Fix A: Compliance `context7` → `set()` in `tool_lists.py` (live tool dump = 12 tools, matches PHASE_2.md). Fix B: `capture.py` (new `CaptureStore` + `SubagentCaptureMiddleware`) + `orchestrator_runtime.py` lenient `_coerce_report`/`_coerce_finding` (string confidences, dict evidence, `violations` alias) + `review.py` uses `capture.consume_duration` (no more hardcoded 0). Unit-verified against session-11 payloads; sessions 13/14 show real durations (compliance 2796/3093 ms, aggregator 40578/50812 ms). Fix C: `tool_scoping.py` wraps scoped MCP tools, emitting per-owning-agent `tool_call`/`tool_result` to `review_events.log`; session 12 event log proves compliance's real `confluence_search`/`confluence_get_page`/`get_review_context_tool`/`pull_request_read` calls. Plus: bounded retry in `run_review` for transient deepagents structured-output parse failures (the weak model returned an empty native output once — session 12's 500 is now absorbed). New blocker logged: review-model nondeterminism (delegation count, dropped diff in `task` args, citation fidelity) is a model-quality issue for Phase 3, not a code defect.
- *[Demo prep — supervisor demo]* — **Model switched to NVIDIA Nemotron 49B and the live path made to actually produce findings.** `REVIEW_MODEL=nvidia:nvidia/llama-3.3-nemotron-super-49b-v1.5` (NVIDIA free key; the only other entitled model, GLM-5.2, dies on socket timeout after ~25 min — see session 33). OpenRouter free tier exhausted; Groq TPM-limited; Gemini ruled out by user. Root-cause chain fixed live across sessions 34→40:
  1. **Subagent transcript capture** (`capture.py`): `SubagentCaptureMiddleware` now implements `wrap_model_call`/`awrap_model_call` and emits per-subagent `thinking` (reasoning content), `tool_call_attempt` (model-emitted, pre-execution) and `invalid_tool_call` events — proving a `tool_call_attempt` with no matching executed `tool_call` means the runtime rejected it. This was the diagnostic that pinned the empty-output bug.
  2. **Handoff context** (`prompts/orchestrator.md` + `_build_user_message`): subagents now get BOTH GitHub `owner`/`repo` AND the local `repo_root` path in every `task` description. Session 34's first dispatch passed only the local path (GitHub tools unusable → missing-params loop) and the retry passed only owner/repo (CRG tools got `repo_root="/"` → "Invalid repository root structure"). Session 40: `get_bridge_nodes_tool` succeeded with the real workspace path.
  3. **`REVIEW_MAX_TOKENS` 2000 → 8192** (`.env`): Nemotron burns completion tokens on `reasoning_content`; a long reasoning turn hit the 2000 cap mid-generation and terminated with an empty assistant message (no content, no tool call) → empty subagent report. 8192 fixed it.
  4. **`review_timeout` setting** (new, default 600, env `REVIEW_TIMEOUT`) replacing the hardcoded 240s in `_ensure_review_provider_profile`: session 38 died with "Timeout on reading data from socket" when one free-tier call exceeded 240s.
  5. **Tolerant parser widened** (`_extract_json`, `_findings_list`, `_coerce_finding`): strips markdown code fences AND `<subagent_report>`-style XML wrappers; recurses into nested report dicts (`{"security_review": {"findings":[...]}}`); maps `security_findings`-style keys, `recommendations`→`recommendation`, `location`→`evidence`, `risk_score`→confidence. Live report shapes drifted across all four runs (markdown-fenced, `security_findings`, nested, XML-wrapped) — all four now unit-tested. Per-agent `AgentExecution` rows finally carry real findings (session 40 security row: 3 findings).
  6. Orchestrator prompt gains "do not re-invoke a subagent that already returned a report" — session 37+ single dispatch (was double, 16.7 min → ~8 min).
  Evidence: sessions 34–40 in `phase1_metadata.db`; session 40 = single dispatch, 4 real tool categories (2 GitHub + CRG `get_bridge_nodes_tool` + file fetch), security + aggregator audit rows populated, 200. Remaining for the demo: the full 4-subagent `review` request (`request_type=review`), then `any_question` variants.
- *[User-approved extension]* — **`compliance_question` and `performance_question` request types added** (symmetry: every specialist now has a direct single-agent request type, matching the existing `security_question`/`impact_question`). `routing_policy.py` adds `"compliance_question": ["compliance"]` and `"performance_question": ["performance"]`; `review` and `any_question` stay (deterministic full pipeline vs free-form pool). No other code changes needed — the orchestrator's "required subagents" path already handles single-element lists, and `compliance`/`performance` already carry their full tool sets. `PHASE_2.md` Routing Policy YAML + DoD synced.
- *[User-approved extension — solution A]* — **The optional `question` field now steers the single-specialist typed requests too** (compliance_question / security_question / performance_question / impact_question), not just `any_question`. `orchestrator_runtime._build_user_message` appends a "Question from the user:" section for `_QUESTION_CARRYING_TYPES` (the four single-specialist types; `review` and `explain_question` intentionally excluded — free-form steering there stays via `any_question`); `prompts/orchestrator.md` instruction #1 now says to forward that question verbatim into the task description so the specialist investigates exactly what was asked (e.g. a Jira ticket key like CLIP-3). Doc sync: `api/models.py` comment, `PHASE_2.md` `ReviewRequest` + Routing Policy comments. No new tools/libraries/safety-rule changes.
- *[System]* — **Layer-discipline repair of the review pre-flight + routing use-cases.** `application/review_service/prepare_review_context.py` was a Layer 2/5 hybrid (inline `select(RepoWorkspace)`, a `_GraphSnapshotStatusQueryPort` shim, and `fastapi.HTTPException` leaks) and `run_review.py` leaked `fastapi` into Layer 3. Now: `PrepareReviewContextService` (pure Layer 3, depends only on two new Layer 4 Protocols in `domain/review/review_context_ports.py`); the SQL moved to two new Layer 5 adapters (`infrastructure/db/repo_workspace_repository.py` → `RepoWorkspaceQueryPort`, `infrastructure/db/graph_status_repository.py` → feeds `GraphReadinessService`); app-layer exceptions in `application/review_service/errors.py` (`RepoNotFoundError`/`GraphNotReadyError`/`UnknownRequestTypeError`); the Layer 2 route is now the composition root and translates them to 404/425/400. **Behavior preserved exactly** — re-verified live via TestClient (temp DB, since the running uvicorn holds the dev DB lock): 400 `Unknown request_type: bogus`, 404 `Unknown repo_id: ghost/app`, 425 `Graph not ready for this commit yet`. Added `tests/test_review_context.py` (5 unit tests with fake ports); full suite 20/20 PASS. No Phase 1 file touched (`GraphReadinessService`, `engine.py`, `models.py`, `routing_policy.py` unchanged); the `async/sync` blessed-exception note in `AGENTS.md` and the `PHASE_2.md` pre-flight section + folder tree updated to match.
## 🚧 Branch-Aware Graph Management (addendum to Phase 2) — IMPLEMENTED, PENDING REVIEW
- **Implemented end to end** per `Branch-Aware Graph Management.md` (§0/§13). Pre-flight verification completed FIRST (both gates green):
  - §1a `list_branches` verified live against the GitHub MCP server (tool name, `{owner, repo}` required input, `[{name, sha, protected}]` response shape — commit SHA field is `sha`).
  - §1b CRG worktree isolation verified via Context7 (`code-review-graph` docs): each worktree root carries its own `.code-review-graph/` DB; `CRG_DATA_DIR`/`--data-dir` only redirect externally. → **no `CRG_DATA_DIR` change needed** (§12 "Not set" holds).
- **Schema migration (§7):** `RepoWorkspace` is now per-`(repo_id, branch)` — `UNIQUE(repo_id, branch)` table constraint (via `sqlmodel`'s `UniqueConstraint`, verified exported in 0.0.39), `repo_id` demoted to a plain index (`ix_repoworkspace_repo_id`), new `branch` (required) + `last_requested_at` columns. `_rebuild_repoworkspace()` in `db/engine.py` runs the 4-step SQLite rebuild (create/insert/drop/rename) inside one transaction, guarded on the `branch` column's presence, backfilling `branch` deterministically via `detect_branch(local_path)` (raises `RuntimeError` rather than guess) and `last_requested_at` from `updated_at`. **Verified on a WAL-consistent copy of the real dev DB** (raw `shutil.copy2` is NOT WAL-safe — the live DB runs `journal_mode=wal`): all 4 rows backfilled (`main`/`master`/`main`/`main`), composite unique + index present, ORM read-back correct, idempotent on 2nd `init_db()`.
- **Ports/adapters (§2–§4):** `RepoWorkspaceQueryPort` gains `get_by_repo_id_and_branch` + `repo_is_registered` (`get_by_repo_id` is deliberately scoped to the default-branch/base-clone row via `id.asc()` to preserve Phase 1 semantics); `RepoSourcePort` gains `create_worktree`/`update_worktree`; `git_repo_source.py` implements them plus module fn `detect_branch`/`current_branch`; `repo_workspace_repository.py` rewritten with `add_worktree_row`/`update_synced_commit`/`touch_requested_at`/`delete_branch_row`/`record_graph_snapshot`.
- **App services (§5, §9):** new `application/repo_ingestion_service/ensure_branch_worktree.py` (`EnsureBranchWorktreeService` + `WorkspaceStore` Protocol; `_build_or_update` = create_worktree→row→build, or update_worktree→update with force-push→build fallback when not ready); `PrepareReviewContextService.execute(repo_id, graph_commit_hash, branch=None)` now 404s unregistered repos and 425s when `branch` is unknown or `last_synced_commit != graph_commit_hash`.
- **Locks/worktree layout (§4):** `acquire_workspace_lock` gained a `branch` param (default-branch key `.{safe}.lock` unchanged so Phase 1 webhooks stay valid; branch key `.{safe}_{safe_branch}.lock`); new probe `try_acquire_lock`; `resolve_worktree_path = {workspace_root}/{safe_id}__{safe_branch}` (worktrees are SIBLINGS of the base clone, not nested — verified `git worktree add` into a nested path pollutes the base working tree).
- **Route (§5, §13):** `POST /review` takes optional `graph_commit_hash` + `branch` (exactly-one-required 400), resolves branch→commit via the live GitHub MCP `list_branches` (404 `BranchNotFoundError`), and on 425-with-branch dispatches `EnsureBranchWorktreeService` as a FastAPI `BackgroundTasks` task. `webhooks.py` is branch-aware (webhook `refs/heads/<b>`; no-op unless it's the default-branch row), converts the old `000...0` early-return into a real cleanup (`git worktree remove` + `prune` + `delete_branch_row`, GraphSnapshot left intact), and adds read-only `GET /repos/{repo_id}/branches`. `WorkspaceEvictionService` is worktree-aware (LRU by `last_requested_at`, `git worktree remove` before `rmtree`; existing `max_gb=10.0` unchanged) and now wired to run once in the lifespan off the event loop.
- **Verification so far:** unit suite 22/22 PASS (`test_review_context.py` fakes extended + branch-path tests); full import chain of every changed module + app startup path OK; migration verified against a WAL-safe copy (NOT yet run on the real dev DB — production copy only).
- **Deviations (log-only, no spec conflict):**
  - **D-8:** `RepoSourcePort` carries THREE new methods (`create_worktree`, `update_worktree`, `current_branch`) — the addendum §3 names only two; `current_branch` was added as a supporting read for the migration backfill and `clone_repository` and is a plain git read (no behavior risk). No new abstraction beyond the spec.
  - **D-9:** The route wires a module-level `EnsureBranchWorktreeService` instance and dispatches `_ensure_worktree.execute(...)` via `BackgroundTasks`, instead of a spec-named module fn `ensure_branch_worktree_and_graph(...)`. Functionally equivalent (composition root wires the service); the spec's name does not exist as a symbol.
  - **D-10 (note):** Branch identity for a non-default worktree row is the requested branch string, not resolved to its remote commit at write time (the worktree checkout fixes the commit); `last_synced_commit` records the built commit, consistent with Phase 1's webhook row semantics.
- **Deviations (log-only, no spec conflict):**
  - **D-8:** `RepoSourcePort` carries THREE new methods (`create_worktree`, `update_worktree`, `current_branch`) — the addendum §3 names only two; `current_branch` was added as a supporting read for the migration backfill and `clone_repository` and is a plain git read (no behavior risk). No new abstraction beyond the spec.
  - **D-9:** The route wires a module-level `EnsureBranchWorktreeService` instance and dispatches `_ensure_worktree.execute(...)` via `BackgroundTasks`, instead of a spec-named module fn `ensure_branch_worktree_and_graph(...)`. Functionally equivalent (composition root wires the service); the spec's name does not exist as a symbol.
  - **D-10 (note):** Branch identity for a non-default worktree row is the requested branch string, not resolved to its remote commit at write time (the worktree checkout fixes the commit); `last_synced_commit` records the built commit, consistent with Phase 1's webhook row semantics.
- **Verification against the real dev DB (done):** migration ran at app startup (restarted uvicorn with new code) — all 4 rows backfilled `main`/`master`/`main`/`main`, composite unique + index present. Live E2E on `/api/v1`: `GET /repos/{repo_id}/branches` 200 with real branch list (route needed `{repo_id:path}` since repo_id = `owner/repo` contains a slash — spec's literal `/repos/{repo_id}/branches` kept); 400 neither/both; 404 unknown branch (`BranchNotFoundError`) and unregistered repo; 425 unready hash.
- **Live happy-path E2E — DONE (post remote fix):** `POST /review` `branch=update-3.0` on `psf/requests` returned 425, and the FastAPI `BackgroundTasks` build ran to completion: git worktree created under `workspaces/psf_requests__update-3.0` (registered in the base clone's `worktree list`, HEAD = branch tip `190a6855...`), `RepoWorkspace` row added `(id=7, psf/requests, update-3.0, ...__update-3.0, 190a6855...)`, CRG full build (`build_or_update_graph_tool` → `built_on_branch: update-3.0`, `head_matches_build: true`; graph DB present at `.code-review-graph/`), and a `GraphSnapshot` row recorded `status=ready`. `bug/5671` (id=6) built the same way. The spec's branch-build flow (§5) works end to end.
- **BUG FOUND + FIXED (D-11):** the route originally dispatched the worktree build via `background_tasks.add_task(...)` and then `raise HTTPException(425)`. FastAPI only attaches `BackgroundTasks` to the response on the **normal return** path (`_build_response_args` in `fastapi/routing.py`); when the endpoint ends in `raise HTTPException`, the exception handler builds a fresh `JSONResponse` with no background, so the build task was silently dropped (verified: live POSTs left no row/snapshot; direct service invocation worked). Fix in `review.py`: return a `JSONResponse(status_code=425, ...)` with `response.background = background_tasks` set instead of raising. Behavior preserved (client still sees 425 + same detail); the background task now actually runs. Re-verified live: the 425-triggered build completed.
- **Remaining for sign-off:** reviewer audit of the addendum.

### Frontend-Phase Notes (recorded now for the React frontend — do not build here)

These three points are the frontend contract for the branch-switch UI, captured during the Branch-Aware work so they are not reverse-engineered later:

- **Required — the branch-switch dropdown must be populated from `GET /repos/{repo_id:path}/branches` (the GitHub-backed proxy), NOT from `RepoWorkspace`.** That endpoint returns *every remote branch* (`[{name, sha, protected}]`); `RepoWorkspace` only ever holds the branches that have actually been reviewed (the default-branch row after `POST /repos`, plus one row per branch a review was requested on). A dropdown built from `RepoWorkspace` would be near-empty right after repo registration, which is why the endpoint — not the table — is the source of truth. First-time click on an unbuilt branch → 425 → background worktree build → poll `POST /review` until 200.
- **Required verification (before the frontend phase) — confirm whether the GitHub MCP `list_branches` tool paginates.** If it caps the result set (e.g. ~30 branches), a large repo would silently show a partial dropdown. Verify the live tool name with `mcpcurl tools --help` (AGENTS.md standing rule) and, if pagination is confirmed, handle it inside `list_repo_branches` (`branch_resolution.py`) before the UI depends on completeness.
- **Optional — add a readiness probe so the UI does not poll `POST /review`.** Today the only way to distinguish "graph still building" (425) from "done" (200) is to re-submit the full review payload, which re-runs the pre-flight and could re-queue work. A lightweight read-only probe — e.g. `GET /api/v1/repos/{repo_id}/branches/{branch}/readiness` → `{"ready": true, "commit": ...}` or `{"ready": false}` — would let the UI poll cheaply without side effects.

## 📝 Changelog / Progress Notes
- *[System]* — **Aggregator report-loss bug fixed (session 50).** When compliance correctly produced real findings (camelCase `validatePDFPath`, `print()` debug, ticket-scope mismatch vs CLIP-3), `POST /review` returned `findings: []` because deepagents delivered the aggregator report as plain final-message content instead of a native `structured_response`, and `_parse_aggregated` had no fallback (it returned a hardcoded empty `AgentOutput`). Added `_parse_aggregated_from_messages`: on missing/empty `structured_response`, it scans the last `AIMessage` content through the existing lenient path (`_extract_json` + `_coerce_report`), mirroring `_parse_tool_message`. Regression-tested with the literal session-50 payload (2 findings recovered). Same root cause as the logged review-model nondeterminism (weak-model output variance), now absorbed by the pipeline. Diff truncation in orchestrator task descriptions remains a prompt-level nuisance (compliance self-recovered via `get_file_contents`); orchestrator prompt now explicitly forbids abbreviating/eliding the diff.
- *[System]* — **Pre-Phase-4 E2E: context agent verified live end-to-end (sessions 116/117).** Seeded a conversation (id 5, `psf/requests`, 3 messages incl. a distinctive "CLIP-4 approved mitigation" fact), then `POST /review` (`any_question`, `graph_commit_hash=190a6855…`, `conversation_id=5`, `user_id=alice`): orchestrator called `search_messages` 5× (all `status=ok`), real DB rows returned (`"CLIP-4 regression"` → message #17; `"CLIP-4"` → #16/#17/#18, snippets verbatim-matched), findings cited `message #17`/`#18` as evidence, `dispatched_agents=[]` (by-design direct answer). **Incident (session 115):** first run 500'd — root cause was the atlassian launch env (see ops blocker above), fixed by relaunching via `run_atlassian_server.sh`. **Dev-DB cleanup:** removed test conversations 1–4 + sessions 113/114/115 + their AgentExecution rows; kept conversation 5 + sessions 116/117 + all audit evidence. Pre-clean backup: `data/backup_phase1_metadata_20260818_103830.db`.
- *[System]* — **Phase 4 Layer 3 (domain_architect pass) complete: durable-summarizer upgrade (PHASE_4.md §5.3).** `application/conversation_service/summarize_conversation.py` gains a keyword-only `llm_summarizer: Callable[[list[str]], Awaitable[str]] | None = None` (new private helper `_summarize_with_fallback`): when injected, it is attempted first; on ANY exception or empty/None result the deterministic tail-summary v1 (`_summarize`, unchanged) is used — a memory-summary failure never fails the review. v1 callers passing only `conversation_id/store/recent_messages` still work unchanged. No port/entity/infra changes needed (contract satisfied by the existing `summarized_up_to_message_id` = latest message id; no blocker logged). Verified inline: LLM-raise→fallback, LLM-blank→fallback, LLM-text→used (stripped), no-injection compat, empty-run→None, single-message + empty-store edge cases; `tests/test_conversation_phase3.py` 9/9 PASS. `infra_engineer` owns `tests/test_memory_phase4.py` + the `orchestrator_runtime.py` wiring.
- *[System]* — **Phase 4 Layer 5/2 (infra_engineer pass) COMPLETE: memory store + LangMem wiring + truncation fix + durable summaries. Full suite `72 passed` (62 baseline + 10 new), all changed/new module imports OK.**
  - **PHASE-4 TOP PRIORITY (truncation fix, §0):** `tool_scoping.py` — deleted `_TOOL_RESULT_MAX_CHARS`/`_truncate_result`; the event wrapper now `return result` FULLY untruncated to the caller (the audit path `json.loads` the payload), while the event-log copy stays capped at 2000 chars + `...(truncated)` suffix (`_truncate`, observational only, not model input). The >4000-char large-recall `invalid_response` misreporting is fixed at the source; regression test `test_event_wrapper_returns_full_result_but_truncates_event_log` + `test_audited_context_tool_parses_large_recall_as_ok` (25-hit, 5000-char payload → `status=ok`, count=25). The old Blockers entry for this is now RESOLVED (struck below).
  - **Config (§4.1):** `config.py` adds `summarization_trigger_tokens: int = 222822` (85% of 262,144 ctx) + `summarization_keep_tokens: int = 26214` (10%).
  - **Memory store (§6):** NEW `memory_store.py` `build_memory_store()` → `aiosqlite.connect(settings.metadata_db_path)` + `PRAGMA journal_mode=WAL`/`busy_timeout=5000`/`foreign_keys=ON` + `AsyncSqliteStore(conn)` (NO index/embeddings) + `await setup()`. Constructed ONCE in the async lifespan → `app.state.memory_store` (single store per process; bindings verified). `AsyncSqliteStore` has no `close()` — tests close `store.conn` directly.
  - **LangMem tools (§6.1/6.2):** NEW `memory_tools.py` — `SHARED_MEMORY_NAMESPACE = ("memories","shared","{user_id}","{repo_id}")`; `build_shared_memory_tools()` + `build_private_memory_tools(agent_name)` → `("memories","private","{user_id}","{repo_id}",agent_name)`. Placeholders resolve via LangGraph `config.configurable` at runtime (identity NEVER a tool arg; `manage_memory` is the only write tool). All 5 subagent runtimes append shared + private tools; orchestrator root always gets shared (search_messages additional when conversation present — root is never tool-less).
  - **Orchestrator wiring (§5):** `run_review` builds ONE `StateBackend()`, adds an EXPLICIT `SummarizationMiddleware(model=settings.review_model, backend=backend, trigger=("tokens", ...), keep=("tokens", ...))` — deepagents replaces the auto one by `.name` in place (verified: exactly 1 in the merged stack, never 2; graph compiles). `create_deep_agent(..., backend=backend, store=self._memory_store)`; identity via `run_config={"configurable": {"user_id": ..., "repo_id": ...}}` threaded through `_run_with_retry(config=...)`. Durable summary: `_write_durable_conversation_summary` (fetches recent messages via `list_messages` in `asyncio.to_thread`, calls `summarize_conversation` with injected `_build_llm_summarizer` = `init_chat_model(settings.review_model)` + one-shot HumanMessage) — runs AFTER a successful review inside try/except, so a summary failure NEVER fails the review (§5.3).
  - **Tests:** NEW `tests/test_memory_phase4.py` (10 tests) — namespace isolation (shared/private, cross-user/cross-repo/cross-agent invisible), namespace-template resolution (via `NamespaceTemplate` closure cells → exact Phase 4 tuples), constants + middleware thresholds (stored in `_lc_helper`), exactly-one-summarization, `build_memory_store` path/index-config, truncation fix, audited large-recall, durable-summary row persistence (temp engine injected by patching the orchestrator's `SQLModelConversationRepository` name — the helper's default engine arg is bound at import, so the home-module patch would NOT redirect it; use the orchestrator-namespace patch), DB-error contract. `test_context_agent_review_integration.py` 4 `BuildRootToolsTest` tests updated to Phase 4 semantics (memory tools always present; search_messages additive). Full suite: **72 passed** (was 62 baseline). Imports OK across all 13 changed/new modules.
  - **Deviations (log-only):** (1) `SummarizationMiddleware` public `trigger`/`keep` live on `_lc_helper` (deepagents 0.7 delegates to langchain's inner helper) — tests assert there; (2) `AgentInput`/`Message` seeding in durable-summary tests uses the ORM models (`infrastructure.db.models`) against the temp engine, matching `test_conversation_phase3.py` patterns. **Next:** reviewer audit of the Phase 4 infra pass.
- *[System]* — **Post-audit review fixes (2026-08-19): F1 + F2 + F5 implemented, F3/F4 accepted as documented tradeoffs. Full suite now `74 passed` (72 + 2 new).**
  - **F1 (ProviderProfile bypass):** `_build_llm_summarizer` now calls `init_chat_model(settings.review_model, max_tokens=settings.review_max_tokens, timeout=settings.review_timeout)` — the deepagents `ProviderProfile` (`_ensure_review_provider_profile`) only applies to models resolved THROUGH `create_deep_agent`, so the summarizer's direct path was uncapped (free tier rejects the full window → silent deterministic fallback) and unbounded (hung provider not limited by `review_timeout`). Verified via Context7 that `init_chat_model` forwards both kwargs. New test pins `max_tokens`/`timeout` on the lazy model construction.
  - **F2 (inline latency):** durable summary moved OFF the request path. `OrchestratorRuntime.run_review` no longer awaits `_write_durable_conversation_summary`; new public guard method `write_durable_conversation_summary(review_input)` (no-op when `conversation_id` is None; absorbs helper exceptions into `logger.warning`, so the background task never surfaces an unhandled error). The review route schedules it via `background_tasks.add_task(...)` on the success path — Starlette runs async background callables post-response. "Never fails the review" holds by construction. New test pins the guard swallows DB errors while the module-level helper still raises (existing helper contract test untouched). Deviation **D-P4-4** logged. **Accepted tradeoff:** background execution means the summary can be silently skipped on client-disconnect-before-send or worker-restart-between-send-and-completion; the durable summary is best-effort/non-critical, so no pending-summary/retry mechanism is added in Phase 4.
  - **F5 (async/sync boundary):** `summarize_conversation` wraps `_latest_message_id` and `store.add_memory_summary` in `asyncio.to_thread` (D-P4-1 now fixed). Double message read remains as a trivial documented inefficiency (both reads off-loop).
  - **F3 (token amplification):** ACCEPTED — deliberate §0 tradeoff; the 222,822-token SummarizationMiddleware owns model-facing token control; truncating the returned result would re-break the audit `json.loads`.
  - **F4 (memory writes across `_run_with_retry` retries):** ACCEPTED — langmem keys by content hash (dedup), writes are best-effort, and the memory store is the Phase 4 bounded write-tool exception.
  - Verified: full suite `74 passed` + 14 subtests, all changed-module imports OK.
- *[System]* — **Phase 4 reviewer audit COMPLETE (2026-08-19): DoD §8 11/11 PASS, §2 compliance constraints met, no blockers.** Per-item verdict (audit performed against the working tree, full suite `72 passed` re-run):
  - §0 Prerequisite (truncation fix): **PASS** — tool result returned untruncated to caller, event-log copy capped at 2000; 5000-char recall audits `status=ok`/count=25 (tests). Old Blockers entry struck.
  - Requirements: **PASS** — `langmem>=0.0.30,<0.1`, `langgraph-checkpoint-sqlite>=3.1,<4`, `langchain-nvidia-ai-endpoints>=1.4.3,<2` present.
  - middleware.py/capture.py inspected: **PASS** — no custom SummarizationMiddleware in `middleware.py`; existing middlewares untouched; explicit one placed in `orchestrator_runtime.run_review` per §5.2.
  - Explicit SummarizationMiddleware: **PASS** — `trigger=("tokens", 222822)` / `keep=("tokens", 26214)` via `settings`, shared `StateBackend()` to BOTH `create_deep_agent` and the middleware; deepagents replaces the auto instance by `.name` → exactly one summarization node (tested: merged stack length 1, graph compiles).
  - Durable conversation summary: **PASS** — `_write_durable_conversation_summary` runs after successful review when `conversation_id` present, injects `_build_llm_summarizer` (`init_chat_model(settings.review_model)`, no hardcoded model string), persists via `ConversationStorePort.add_memory_summary` → `MemorySummary`; deterministic `_summarize` fallback on LLM/empty; whole helper in try/except → never fails the review (tests: row persists; DB error → helper raises, run_review swallows).
  - AsyncSqliteStore: **PASS** — `langgraph.store.sqlite.AsyncSqliteStore`, exactly one instance built in async lifespan → `app.state.memory_store`, manual PRAGMAs (WAL/busy_timeout=5000/foreign_keys=ON), `await setup()` once, NO index config.
  - Shared memory: **PASS** — `("memories","shared","{user_id}","{repo_id}")` on orchestrator root + all 5 subagents; root is never tool-less.
  - Private memory: **PASS** — `("memories","private","{user_id}","{repo_id}","<literal agent name>")` per subagent; test proves Security cannot read Performance's private memory + cross-repo isolation.
  - Identity via `config.configurable`: **PASS** — threaded through `_run_with_retry(config=...)` → `agent.ainvoke(..., config=config)`, sourced from trusted `AgentInput`; never an LLM tool argument.
  - No embeddings: **PASS** — store built with no `index`; `asearch` is exact-namespace only.
  - Write-tool exception: **PASS** — `manage_memory` flagged in `memory_tools.py` docstring + this tracker as the only write-capable tool, bounded to memory namespaces.
  - Clean architecture: **PASS** — `application/conversation_service/summarize_conversation.py` imports only `collections.abc`/ports/entity (LLM injected as a plain callable); `domain/` untouched (no new entities needed, §6.5); no framework imports in Layer 3/4.
  - **Deviations (log-only, no spec conflict):** **D-P4-1 — FIXED (2026-08-19, review finding F5):** the two sync SQLite calls inside `summarize_conversation` (`_latest_message_id`, `add_memory_summary`) are now `asyncio.to_thread`-wrapped, matching `run_conversation_turn.py`'s application-layer pattern. The redundant double read remains (orchestrator pre-fetches `list_messages` for `recent_messages`, then the use-case lists again for `_latest_message_id`) — both reads are now off the loop, so it is a trivial, documented inefficiency, not a boundary violation. **D-P4-2** — `user_id or "anonymous"` fallback for no-conversation runs: identity still from trusted AgentInput, memory tools only meaningful when a conversation supplies a user_id; acceptable default, noted. **D-P4-3 (informational)** — durable summary covers all messages since conversation start, not strictly "since last summary" (no port reads the prior `MemorySummary`); `summarized_up_to_message_id` is recorded so a future delta is possible. **D-P4-4 (2026-08-19, review finding F2):** durable-summary execution moved OFF the request path — the review route now schedules `OrchestratorRuntime.write_durable_conversation_summary` as a FastAPI `BackgroundTask` (runs post-response). Spec §5.3 named it a post-run step in `orchestrator_runtime.py`; the step stays an orchestrator concern, only its *scheduling* moved to the composition root. **Tradeoff (accepted):** as a Starlette `BackgroundTask`, the summary only executes after the response body sends successfully — a client that disconnects before the send, or a worker restart between send and task completion, skips/interrupts the task silently (no warning, no retry). The durable summary is explicitly best-effort/non-critical (D-P4-3); **no pending-summary or retry mechanism is introduced in Phase 4.** **Next:** commit the working tree so HEAD reflects what was verified (uncommitted per D-6-era note), then Phase 4 sign-off.
